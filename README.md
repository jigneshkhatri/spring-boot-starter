# spring-boot-interceptor-security
Spring boot project, with interceptor to secure endpoints according to organization -> branch (levels) roles and permissions. It also uses Spring boot OAuth2 security.
